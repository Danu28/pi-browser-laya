{
  "name": "pi-browser-laya",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright-based laya-inspired browser extension for pi (3-4 turns) — single dep: playwright (workspace ref, see root package.json)",
  "dependencies": {
    "playwright": "1.63.0"
  },
  "scripts": {
    "postinstall": "npx playwright install chromium"
  }
}
